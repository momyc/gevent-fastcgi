[loggers]
keys = root

[logger_root]
handlers = console
level = DEBUG

[handlers]
keys = console

[handler_console]
class = StreamHandler
args = (sys.stdout,)
formatter = basic

[formatters]
keys = basic

[formatter_basic]
format = %(process)d/%(threadName)s %(levelname)s %(message)s

